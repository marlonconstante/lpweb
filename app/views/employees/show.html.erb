<p id="notice"><%= notice %></p>

<p>
  <strong>Nome:</strong>
  <%= @employee ? @employee.name : "N/A" %>
</p>

<p>
  <strong>Setor:</strong>
  <%= @employee.sector    ? @employee.sector.name : "N/A" %>
</p>

<p>
  <strong>Localiza&ccedil;&atilde;o:</strong>
  <%= @employee.location ? @employee.location.name : "N/A" %>
</p>

<%= link_to 'Editar', edit_employee_path(@employee), :class => 'btn btn-default glyphicon glyphicon-pencil' %>
<%= link_to 'Voltar', employees_path, :class => 'btn btn-default glyphicon glyphicon-arrow-left' %>
